{
  "name": "daily-recipe",
  "version": "1.0.0",
  "description": "每日食谱推荐系统",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "prebuild": "if exist dist rmdir /s /q dist",
    "build": "mkdir dist && xcopy /E /I /Y public dist && xcopy /E /I /Y functions dist\\functions",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.0.8",
    "@hono/node-server": "^1.8.2"
  },
  "devDependencies": {
    "wrangler": "^3.30.1",
    "@cloudflare/workers-types": "^4.20240208.0"
  }
} 